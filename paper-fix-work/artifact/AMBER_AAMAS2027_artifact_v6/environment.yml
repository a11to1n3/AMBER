name: amber-aamas-artifact
channels:
  - conda-forge
dependencies:
  - python=3.12
  - numpy>=1.26
  - matplotlib>=3.8
  - pip
  - pip:
      - polars>=1.0
      - networkx>=3.0
      - seaborn>=0.13
      - scikit-optimize>=0.9
      # Optional GPU / baselines (Host-B campaign used these; figures do not require them):
      # - cupy-cuda12x
      # - mesa
      # - mesa-frames
      # - agentpy
      # - Melodie
      # - simpy
      # - pyflamegpu  # from https://whl.flamegpu.com/whl/cuda130/
